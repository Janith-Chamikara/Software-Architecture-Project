# Traffic Fine System - Backend REST API

This is NestJS REST API built with TypeScript. It acts as the central engine of the system, handling business logic, database transactions (using an ORM like TypeORM or Prisma), and JWT authentication using guards and strategies. It exposes all the necessary endpoints to validate fine details, process payments, aggregate statistics for the admin dashboard, and trigger SMS notifications.
